# 🤖 Telegram Ads Platform

Платформа для размещения объявлений в Telegram каналах с веб-интерфейсом и админ-панелью.

---

## 🚀 БЫСТРЫЙ СТАРТ

### 1️⃣ Настройка переменных окружения

**ЭТО ВАЖНО!** Перед запуском бота нужно настроить файл `.env` с твоими токенами и паролями.

#### Шаг 1: Создай файл .env

```bash
# Скопируй шаблон
cp .env.example .env
```

#### Шаг 2: Заполни .env своими данными

Открой файл `.env` и заполни следующие поля:

**ОБЯЗАТЕЛЬНЫЕ:**
- `BOT_TOKEN` - получи у [@BotFather](https://t.me/BotFather)
- `API_ID` и `API_HASH` - получи на [my.telegram.org](https://my.telegram.org)
- `DB_PASSWORD` - придумай надёжный пароль для базы данных
- `ADMIN_IDS` - твой Telegram ID (узнай у [@userinfobot](https://t.me/userinfobot))

**Пример заполненного .env:**
```env
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
API_ID=12345678
API_HASH=abcdef1234567890abcdef1234567890
DB_PASSWORD=MyStr0ngP@ssw0rd2024
ADMIN_IDS=123456789
```

⚠️ **ВАЖНО:** Файл `.env` содержит секретные данные! Не отправляй его никому и не загружай в Git!

---

### 2️⃣ Установка зависимостей

```bash
# Создай виртуальное окружение
python3 -m venv venv

# Активируй его
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установи зависимости
pip install -r requirements.txt
```

---

### 3️⃣ Настройка базы данных

```bash
# Примени миграции
alembic upgrade head
```

---

### 4️⃣ Запуск бота

```bash
# Запуск в режиме разработки
python bot/main.py
```

---

## 📁 Структура проекта

```
telegram-ads-platform/
├── bot/                    # Код Telegram бота
│   ├── handlers/          # Обработчики команд и сообщений
│   ├── keyboards/         # Клавиатуры (inline и reply)
│   ├── middlewares/       # Middleware (antiflood, auth)
│   ├── services/          # Бизнес-логика
│   ├── database/          # Модели БД и подключение
│   └── main.py            # Точка входа
├── backend/               # FastAPI backend (в разработке)
├── web/                   # Web интерфейс (в разработке)
├── migrations/            # Миграции Alembic
├── shared/               # Общие конфиги
├── .env                  # ⚠️ Секретные данные (не в Git!)
├── .env.example          # Шаблон для .env
├── .gitignore           # Список игнорируемых файлов
└── requirements.txt      # Python зависимости
```

---

## 🔒 Безопасность

### Что НЕЛЬЗЯ делать:

❌ Отправлять файл `.env` кому-либо  
❌ Загружать `.env` в Git/GitHub  
❌ Делиться токенами и паролями  
❌ Хранить `.env` в облачных сервисах без шифрования  

### Что НУЖНО делать:

✅ Использовать `.env.example` как шаблон  
✅ Хранить `.env` только локально  
✅ Использовать сильные пароли  
✅ Регулярно менять токены (если были скомпрометированы)  

---

## 🛠 Полезные команды

```bash
# Проверить статус бота
./check_bot.sh

# Посмотреть логи
tail -f logs/bot.log

# Остановить бота
pkill -f "python.*bot/main.py"

# Запустить с отладкой
DEBUG=True python bot/main.py
```

---

## 📝 Дополнительная информация

- **Документация aiogram**: https://docs.aiogram.dev/
- **Telegram Bot API**: https://core.telegram.org/bots/api
- **SQLAlchemy**: https://docs.sqlalchemy.org/

---

## 🆘 Поддержка

Если возникли проблемы:
1. Проверь, что все переменные в `.env` заполнены правильно
2. Проверь логи в папке `logs/`
3. Убедись, что PostgreSQL и Redis запущены

---

**Удачи в разработке! 🚀**
