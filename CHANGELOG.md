# Changelog

История изменений проекта Telegram Ads Platform (@proday_main_bot)

---

## 2026-01-22

### Изменено
- **Отказ от архивного канала** (`bot/services/ad_lifecycle.py`)
  - Убрана логика публикации в архивный канал при снятии объявления
  - Данные объявления хранятся в БД, медиа — на серверах Telegram по file_id
  - Метод `move_to_archive` теперь только удаляет из каналов и меняет статус на INACTIVE
  - Метод `republish_from_archive` теперь работает для статусов inactive, deleted, pending
  - Добавлен метод `move_inactive_to_deleted` — перемещение неактивных в удалённые через 30 дней

- **Кнопки в "Мои объявления"** (`bot/handlers/ad_management.py`)
  - **Активные**: Изменить, Удалить (заголовок — ссылка на канал)
  - **Неактивные**: Изменить, Удалить (заголовок — ссылка на просмотр в боте)
  - **На модерации**: Изменить, Опубликовать (заголовок — ссылка на просмотр в боте)
  - **Удалённые**: Опубликовать, Удалить (заголовок — ссылка на просмотр в боте)

- **Кнопка "Удалить"** — убрано слово "навсегда" (`bot/handlers/start.py`)

### Добавлено
- **Замена фото/видео в объявлении** (`bot/handlers/ad_management.py`, `bot/handlers/start.py`)
  - Кнопка «📷 Заменить фото/видео» в меню редактирования
  - Состояние `waiting_for_new_media` для сбора новых медиа
  - Поддержка до 10 фото или 1 видео
  - Меню редактирования теперь показывает текущие фото/видео объявления

- **Просмотр своего объявления** (`bot/handlers/start.py`)
  - Deep link `view_` для просмотра неактивных, на модерации и удалённых объявлений
  - Функция `show_own_ad_preview()` — показывает объявление владельцу со статусом и кнопками
  - Отображение фото/видео и полной информации об объявлении
  - Кнопка «✏️ Изменить» добавлена для всех статусов (включая удалённые)

- **Меню категорий в "Мои объявления"** (`bot/handlers/ad_management.py`)
  - При нажатии "Мои объявления" открывается меню с категориями
  - Категории: Активные, Неактивные, На модерации, Удалённые
  - Каждая категория показывает количество объявлений
  - Пагинация работает в рамках выбранной категории
  - Кнопка "К категориям" для возврата в меню

- **Подсчёт объявлений по статусам** (`bot/database/queries.py`)
  - `get_user_ads_counts_by_status()` — получение количества по каждому статусу
  - `get_user_ads_count()` теперь поддерживает фильтр по статусу

### Исправлено
- **Статусы старых объявлений** (миграция данных в БД)
  - 18 объявлений со статусом "deleted" но с channel_message_ids переведены в "active"
  - 76 объявлений без expires_at получили срок +30 дней от текущей даты

- **Ошибка `name 'select' is not defined`** в обработчиках кнопок "Продлить" и "Снять" (`bot/handlers/ad_management.py`)
  - Добавлены недостающие импорты `select`, `Ad`, `AdStatus` в callback-обработчики

- **Множественные уведомления** — исправлена логика выбора объявлений для уведомлений (`bot/services/ad_lifecycle.py`)
  - Теперь каждое объявление получает ровно 3 уведомления: за 2 дня, за 1 день, за 1 час
  - Исправлен запрос: объявления выбираются в конкретном временном окне, без перекрытий

- **Архивация в бота вместо канала** — исправлен ID архивного канала (`shared/regions_config.py`)
  - Был указан user_id вместо channel_id
  - Теперь архив корректно публикуется в приватный канал `-1003620946677`

- **HTML-код в уведомлениях и архиве** — добавлен `parse_mode='HTML'` (`bot/services/ad_lifecycle.py`)
  - Уведомления об истечении срока теперь корректно форматируются
  - Сообщения в архивном канале отображаются с форматированием

---

## 2026-01-21

### Добавлено
- **Система автоподнятия и уведомлений о продлении** — полная переработка жизненного цикла объявлений
  - Срок публикации: 30 дней
  - Уведомления: на 28-й день, 29-й день и за 1 час до истечения
  - Кнопки "Продлить" и "Снять" в уведомлениях

- **Услуги поднятия объявлений** (`bot/config/pricing.py`)
  - `boost_now` — мгновенное поднятие (100₽)
  - `boost_4x_6d` — 4 поднятия раз в 6 дней (300₽)
  - `boost_2x_10d` — 2 поднятия раз в 10 дней (150₽)

- **Новые поля в модели Ad** (`bot/database/models.py`)
  - `boost_service` — выбранная услуга автоподнятия
  - `boost_remaining` — осталось автоподнятий
  - `next_boost_at` — время следующего автоподнятия
  - `last_extended_at` — время последнего продления
  - `notifications_sent` — отправленные уведомления (JSONB)
  - Индекс `idx_ad_next_boost` для поиска объявлений на автоподнятие

- **Миграция** `migrations/versions/20260121_add_boost_fields.py`
  - Добавляет все новые поля для автоподнятия и уведомлений

- **Проверка лимита публикаций** (`bot/utils/limits.py`)
  - `can_publish_ad()` — проверка лимита публикаций за 30 дней
  - `get_publication_stats()` — статистика публикаций пользователя
  - `format_limits_info_async()` — расширенная информация о лимитах
  - Лимит для free: 30 публикаций/30 дней
  - Лимит для business: 90 публикаций/30 дней
  - Продление объявления НЕ считается новой публикацией

### Изменено
- **Лимиты и тарифы** (`bot/config/pricing.py`)
  - `max_active_ads`: 90 (free) / 300 (business)
  - `max_publications_per_30d`: 30 (free) / 90 (business)
  - `ad_duration_days`: 30 дней для всех
  - Бизнес-подписка: 999₽/мес
  - Бизнес-пакет: 2499₽ на 90 дней
  - Цена за доп. объявление/публикацию: 10₽ (free) / 7₽ (business)
  - Бизнес включает: 3 boost_now, 1 boost_4x_6d, 1 boost_2x_10d

- **Сервис жизненного цикла** (`bot/services/ad_lifecycle.py`)
  - `extend_ad()` — продление = удаление старого + публикация нового
  - `boost_ad()` — поднятие с учётом автоподнятия
  - `get_ads_for_notification()` — поиск объявлений для уведомления по дням
  - `get_ads_for_final_notification()` — поиск объявлений для финального уведомления (за 1 час)
  - `send_expiry_notification()` — отправка уведомлений с inline-кнопками
  - `process_auto_boosts()` — обработка запланированных автоподнятий

- **Обработчики callback-кнопок** (`bot/handlers/ad_management.py`)
  - `callback_extend_ad` — обработка кнопки "Продлить"
  - `callback_archive_ad` — обработка кнопки "Снять"
  - `callback_boost_ad` — обработка кнопки поднятия

- **Фоновая задача** (`bot/main.py`)
  - Проверка истёкших объявлений: каждый час
  - Обработка автоподнятий: каждый час
  - Уведомления 28/29 день: в 10:00 МСК
  - Финальное уведомление (за 1 час): каждые 15 минут
  - Архивация старых inactive: в 3:00 МСК

---

## 2026-01-20

### Добавлено
- **Система архивных каналов для хранения медиа** — решена проблема хранения фото неактивных объявлений
  - Архивный канал на каждый регион (`shared/regions_config.py`)
  - Поля в модели Ad: `archive_message_ids`, `archived_to_channel_at`, `republish_count`, `last_republished_at`
  - Миграция: `migrations/versions/20260120_add_archive_fields.py`

- **Сервис управления жизненным циклом** (`bot/services/ad_lifecycle.py`)
  - `move_to_archive()` — перемещение в архивный канал при истечении срока
  - `republish_from_archive()` — переопубликация из архива
  - `archive_old_inactive()` — полная архивация через 6 месяцев
  - `send_expiry_notification()` — уведомление об истечении срока

- **Фоновая задача `ad_lifecycle_task`** (`bot/main.py`)
  - Проверка истёкших объявлений (каждый час)
  - Уведомления за 1 день до истечения (в 10:00 МСК)
  - Полная архивация старых (в 3:00 МСК)

- **Переопубликация объявлений** (`bot/handlers/start.py`)
  - Deep link: `start=republish_{ad_id}`
  - Кнопка "🔄 Опубликовать заново" в списке неактивных объявлений
  - Первая переопубликация бесплатно (настраивается)
  - Проверка баланса и списание для платных переопубликаций

- **Услуги автоподнятия** (`bot/config/pricing.py`)
  - `auto_republish_7d` — автоподнятие каждые 7 дней (99₽)
  - `auto_republish_3d` — каждые 3 дня (199₽)
  - `auto_republish_1d` — ежедневное (399₽)

- **Конфигурация жизненного цикла** (`bot/config/pricing.py`)
  - `AD_LIFECYCLE_CONFIG` — настройки переопубликации и уведомлений

### Изменено
- **Статусы в "Мои объявления"** (`bot/handlers/ad_management.py`)
  - Добавлен статус 💤 для неактивных объявлений
  - Кнопка переопубликации для inactive объявлений

- **Лимиты объявлений** (`bot/utils/limits.py`, `bot/database/queries.py`)
  - В лимит считаются только `ACTIVE` и `PENDING`
  - Неактивные объявления не занимают лимит
  - В список "Мои объявления" добавлены `INACTIVE` и `NEEDS_EDIT`

- **Новые статусы объявлений** (`bot/database/models.py`)
  - `ACTIVE` — активное (опубликовано, срок не истёк)
  - `INACTIVE` — не активное (срок публикации истёк)
  - `NEEDS_EDIT` — требует редактирования (отклонено модератором)
  - `DELETED` — удалено пользователем (хранится 6 месяцев)
  - `ARCHIVED` — в архиве (старые удалённые)
- **Поле `deleted_at`** в модели Ad — время удаления объявления
- **Архивная таблица `archived_ads`** — для хранения удалённых объявлений старше 6 месяцев
  - Хранит копию данных объявления
  - Индексы по user_id и archived_at

### Удалено
- **Проверка фото при создании объявления** — перенесена в отдельного бота-модератора
  - Удалены файлы: `bot/services/local_moderation.py`, `bot/services/vision_ocr.py`
  - Удалены пакеты: EasyOCR, NudeNet, PyTorch, torchvision (~6 GB)
  - Удалена функция `check_photos_for_forbidden_text()` из `ad_creation.py`
  - Публикация объявлений теперь мгновенная
  - Модерация фото будет выполняться отдельным ботом на другом сервере

### Исправлено
- **Ложные срабатывания детекции телефонов** (`bot/utils/content_filter.py`)
  - Убран слишком общий паттерн `\d{10,11}` — ловил даты, артикулы, серийные номера
  - Новые паттерны: `[78]9\d{9}` (мобильные с кодом) и `9\d{9}` (без кода)
- **Обработка ошибки "chat not found"** (`bot/handlers/start.py`)
  - Добавлен перехват TelegramBadRequest для несуществующих чатов
  - Ошибки больше не засоряют логи


---

## 2026-01-19

### Изменено
- **Переход с Claude API на YandexGPT** — решена проблема блокировки Anthropic из России
  - LLM-модерация теперь использует YandexGPT Pro (`bot/utils/llm_moderation.py`)
  - AI-описания теперь используют YandexGPT Pro (`bot/services/ai_description.py`)
  - Новые настройки в `.env`: `YANDEX_GPT_API_KEY`, `YANDEX_GPT_FOLDER_ID`, `YANDEX_GPT_MODEL`
  - Обновлён `bot/config/settings.py` — убраны Claude настройки, добавлены YandexGPT
  - Код упрощён: убран subprocess, прямые вызовы API через httpx

### Добавлено
- **Проверка текста на фото через Yandex Vision OCR**
  - Новый сервис `bot/services/vision_ocr.py` — распознавание текста на изображениях
  - При публикации объявления все фото проверяются на запрещённый контент
  - Текст с фото проходит через rule-based фильтр (телефоны, ссылки, мат и т.д.)
  - Отдельный API ключ `YANDEX_VISION_API_KEY` в `.env`
  - Интеграция в `bot/handlers/ad_creation.py` — функция `check_photos_for_forbidden_text()`
  - Если на фото найден запрещённый текст — объявление отклоняется с возможностью редактирования

- **AI-улучшение описаний объявлений** — помощь в написании описаний с помощью YandexGPT
  - Сервис `bot/services/ai_description.py` — улучшение текста через YandexGPT Pro
  - После ввода описания пользователь видит кнопки: [✅ Далее] [✨ Улучшить с ИИ]
  - AI сохраняет всю информацию от пользователя, исправляет орфографию, делает текст читаемым
  - Результат показывается в `<code>` для удобного копирования и редактирования
  - Цикл редактирования: можно улучшать несколько раз или отредактировать вручную
  - Новое состояние `AdCreation.description_ai_pending` для обработки цикла
  - Настройки: `AI_DESCRIPTION_ENABLED`, `AI_DESCRIPTION_DAILY_LIMIT` в settings.py
  - Клавиатуры: `get_description_confirm_keyboard()`, `get_ai_description_result_keyboard()`

---

## 2026-01-18

### Улучшено
- **Детальное логирование ошибок Claude API** (`bot/utils/llm_moderation.py`)
  - Добавлен метод `_format_api_error()` для форматирования ошибок
  - Логирование HTTP статус-кодов с понятными описаниями (401, 403, 429, 500, etc.)
  - Парсинг JSON-ответа от API для извлечения `error.type` и `error.message`
  - Отдельная обработка таймаутов (`httpx.TimeoutException`)
  - Теперь в логах видно конкретную причину ошибки API (неверный ключ, rate limit, billing и т.д.)

- **Детальное логирование платежей Robokassa** (`bot/services/robokassa.py`, `bot/main.py`)
  - Добавлен класс `PaymentLogger` со структурированным логированием
  - Логирование IP-адреса для всех платёжных операций (безопасность)
  - Логирование всех параметров webhook (без подписей)
  - Логирование баланса до/после пополнения (аудит)
  - Логирование времени обработки запроса (мониторинг)
  - Отдельный логгер `payments.robokassa` для удобной фильтрации
  - Формат: `[PAYMENT] SUCCESS | inv_id=123 | user=456 | amount=500.00₽ | balance: 100→600₽ | IP=x.x.x.x | 45ms`

---

## 2026-01-17

### Исправлено
- **Telegram Stars:** добавлен отсутствующий параметр `provider_token=""` в `send_invoice()` (`bot/handlers/payment.py:74`)
  - Ошибка: "missing 1 required positional argument: 'provider_token'"
  - Для Telegram Stars (XTR) требуется пустой provider_token

### Добавлено
- **Интеграция Robokassa** — приём платежей банковскими картами:
  - Сервис `bot/services/robokassa.py` — генерация ссылок оплаты, проверка подписей
  - Webhook-обработчики в `bot/main.py`: Result URL (POST), Success/Fail URL (GET)
  - Обработчики пополнения в `bot/handlers/payment.py`: deposit_rub, deposit_rub_{amount}
  - Конфигурация: ROBOKASSA_MERCHANT_LOGIN, ROBOKASSA_PASSWORD1, ROBOKASSA_PASSWORD2
  - Алгоритм подписи: SHA256
  - Автоматическое уведомление в бот после успешной оплаты

- **Публичная оферта** — несколько способов доступа:
  - Команда `/offer` — отправляет PDF-файл в чат (`bot/handlers/billing.py`)
  - Кнопка "📄 Оферта" в меню пополнения (`bot/keyboards/billing.py`)
  - Веб-ссылка `https://prodaybot.ru/offer` — открывает PDF в браузере (nginx)
  - PDF файл: `/uploads/oferta.pdf`


- **Система биллинга и платных услуг** (масштабное обновление)

  **Балансы пользователя:**
  - Раздельные балансы: рубли (₽) и Telegram Stars (⭐)
  - Новые поля в User: `balance_rub`, `balance_stars`, `total_spent_rub`, `total_spent_stars`
  - Тип аккаунта: `account_type` (free/pro/business)

  **Новые модели БД** (`bot/database/models.py`):
  - `Transaction` — история всех операций с балансом
  - `ExchangeRate` — курсы валют для Stars
  - `Promocode` — промокоды
  - `PromocodeUsage` — использование промокодов
  - `UserServicePurchase` — купленные услуги

  **Конфигурация** (`bot/config/pricing.py`):
  - Типы подписок: Free, PRO, Business
  - Каталог платных услуг: продление, продвижение, закрепление
  - Лимиты по типу аккаунта (объявления, регионы, ссылки, видео)
  - Легко редактируемые цены без изменения кода

  **Сервисы** (`bot/services/`):
  - `billing.py` — пополнение, списание, возвраты, история
  - `exchange_rate.py` — курс Stars (API ЦБ РФ, обновление в 4:00 МСК)
  - `promocodes.py` — валидация и применение промокодов

  **Обработчики** (`bot/handlers/`):
  - `payment.py` — Telegram Stars оплата, покупка услуг
  - `billing.py` — UI баланса, история, промокоды
  - Команды: `/balance`, `/history`

  **Проверка лимитов** (`bot/utils/limits.py`):
  - Проверка лимита объявлений при создании
  - Проверка доступности ссылок, видео, регионов
  - Интеграция в `ad_creation.py`

  **Клавиатуры** (`bot/keyboards/billing.py`):
  - Меню биллинга, пополнения, выбора валюты

  **Миграция БД:**
  - `8d5d1ecf1222_add_billing_tables_and_user_balance_fields`

- **Кнопка "Баланс" в главном меню** (`bot/keyboards/reply.py`, `bot/keyboards/inline.py`, `bot/handlers/billing.py`)
  - Добавлена кнопка "💰 Баланс" в reply и inline клавиатуры главного меню
  - Быстрый доступ к балансу, истории операций, пополнению

---

## 2026-01-16

### Добавлено
- **Множественные ссылки в объявлениях** (`bot/handlers/ad_creation.py`, `bot/database/models.py`)
  - Шаг 14: выбор количества ссылок (1-4) или пропуск
  - Последовательный ввод названия и URL для каждой ссылки
  - Миграция БД: `link_title`/`link_url` → `links` (JSONB массив)
  - Отображение всех ссылок в превью и публикации

- **Circuit breaker для LLM-модерации** (`bot/utils/llm_moderation.py`)
  - После 3 ошибок API подряд — автоматическое отключение на 5 минут
  - Умное логирование: первая ошибка как ERROR, остальные не чаще раза в минуту
  - Автоматическое восстановление после таймаута
  - Fallback на rule-based фильтр при недоступности API

---

## 2026-01-15

### Исправлено
- **Фильтр контента** (`bot/utils/content_filter.py`)
  - Исправлены ложные срабатывания на слово "канал" (содержит "анал")
  - Улучшена логика проверки названий ссылок

### Изменено
- **Создание объявлений** (`bot/handlers/ad_creation.py`)
  - Доработана валидация контента при создании объявлений

---

## 2026-01-14

### Добавлено
- **LLM-модерация объявлений** (`bot/utils/llm_moderation.py`)
  - Интеграция с Claude API для умной модерации
  - Учёт категории объявления при проверке (контекстная модерация)
  - Настраиваемый порог уверенности

- **Поля для ссылок в объявлениях** (`bot/database/models.py`, миграция)
  - Возможность прикреплять ссылку к объявлению
  - Название ссылки (link_title) и URL (link_url)

- **Antiflood middleware** (`bot/middlewares/antiflood.py`)
  - Защита от спама сообщениями

### Изменено
- **Управление объявлениями** (`bot/handlers/ad_management.py`)
  - Улучшен интерфейс управления объявлениями

- **Стартовый хендлер** (`bot/handlers/start.py`)
  - Обновлено приветственное сообщение

---

## 2026-01-13

### Добавлено
- **Избранное** (`bot/handlers/favorites.py`)
  - Возможность добавлять объявления в избранное

- **Комментарии** (`bot/handlers/comments.py`)
  - Система комментариев к объявлениям

---

<!--
Формат записи:

## ГГГГ-ММ-ДД

### Добавлено (новые функции)
### Изменено (изменения в существующих функциях)
### Исправлено (баг-фиксы)
### Удалено (удалённые функции)

- **Краткое описание** (`путь/к/файлу.py`)
  - Детали изменения
-->
